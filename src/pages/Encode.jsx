import Layout from "../components/Layout";
import EncodeComponent from "../components/encode-decode/Encode.Component";

function Encode() {
  return (
    <Layout>
      <EncodeComponent />
    </Layout>
  );
}

export default Encode;
